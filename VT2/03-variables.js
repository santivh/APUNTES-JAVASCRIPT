// Para definir variables en JS emplearemos var o let
// No es necesario que indiquemos de que tipo va a ser la variable
// Podemos emplear var o let 

var nombre = valor;
var numero = 39;
let cadenaCaracteres = "Hola que tal";
let booleano = true;

// Empleamos const para definir constantes

const sueldo = 1000;